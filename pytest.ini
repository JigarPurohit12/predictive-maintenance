[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    error::FutureWarning
    # shap still calls matplotlib colormap setters that are on their way out.
    ignore:The set_.* function will be deprecated:PendingDeprecationWarning
