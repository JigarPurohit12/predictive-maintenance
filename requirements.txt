# Runtime dependencies.

# Config and contracts
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# Data layer (phases 1-2)
pandas>=2.2
pyarrow>=16.0
numpy>=1.26,<3
duckdb>=1.0          # local stand-in for Athena; the SQL is shared

# Phase 3 — uncomment when you reach modelling.
# scikit-learn>=1.5
# xgboost>=2.1
# shap>=0.46
# matplotlib>=3.9
# mlflow>=2.15

# Phase 4 — uncomment when AWS enters.
# boto3>=1.34
# sagemaker>=2.230
